version: '3.7'

services:
  chatgpt-discord-bot:
    container_name: ai-discord-bot
    image: ghcr.io/poma4ka/ai-discord-bot:latest
    restart: always
    env_file:
      - ./.env
    deploy:
      resources:
        limits:
          memory: 500M
          cpus: '0.5'
